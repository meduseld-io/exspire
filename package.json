{
  "name": "exspire",
  "version": "0.1.0-alpha",
  "description": "ExSpire — Expiry tracking app",
  "private": true,
  "scripts": {
    "release": "commit-and-tag-version --prerelease alpha",
    "release:patch": "commit-and-tag-version --release-as patch --prerelease alpha",
    "release:minor": "commit-and-tag-version --release-as minor",
    "release:major": "commit-and-tag-version --release-as major",
    "release:stable": "commit-and-tag-version"
  },
  "devDependencies": {
    "commit-and-tag-version": "^12.7.0"
  }
}
